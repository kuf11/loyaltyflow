import express from 'express';
import pg from 'pg';
import {verifySession} from './security.js';
const {Pool}=pg,pool=new Pool({connectionString:process.env.DATABASE_URL});
const jwtSecret=String(process.env.JWT_SECRET||'');
const platformAdmins=new Set(String(process.env.PLATFORM_ADMIN_EMAILS||'').split(',').map(v=>v.trim().toLowerCase()).filter(Boolean));
const originalInit=express.application.init,originalUse=express.application.use,originalPost=express.application.post;
let installed=false,schemaReady=false;
async function ensureSchema(){if(schemaReady)return;await pool.query('alter table users add column if not exists session_version integer not null default 0');await pool.query(`create table if not exists platform_admin_audit(id bigserial primary key,actor_id uuid references users(id) on delete set null,actor_email text not null,target text not null,action text not null,http_status int not null,created_at timestamptz not null default now())`);schemaReady=true}
function cookieToken(req){const match=String(req.headers.cookie||'').match(/(?:^|;\s*)lf_session=([^;]+)/);try{return match?decodeURIComponent(match[1]):''}catch{return''}}
function bearer(req){const value=String(req.get('Authorization')||'');return /^Bearer\s+/i.test(value)?value.replace(/^Bearer\s+/i,'').trim():cookieToken(req)}
function denied(user){if(!user||user.admin_frozen)return true;const status=String(user.status||user.account_status||'').toLowerCase();if(['pending_admin','email_pending','blocked','disabled','rejected','frozen'].includes(status))return true;const isPlatform=platformAdmins.has(String(user.email||'').toLowerCase());return !isPlatform&&user.trial_end&&new Date(user.trial_end).getTime()<Date.now()}
async function currentAccount(req,res,next){if(req.method==='OPTIONS'||!req.path.startsWith('/admin-api/v1/'))return next();try{await ensureSchema();const session=verifySession(bearer(req),jwtSecret);if(!session)return res.status(401).json({error:'Войдите в аккаунт'});const user=(await pool.query('select * from users where id=$1',[session.id])).rows[0];if(!user||Number(session.sv||0)!==Number(user.session_version||0))return res.status(401).json({error:'Сессия отозвана. Войдите снова.'});if(denied(user))return res.status(403).json({error:'Аккаунт заблокирован, не активирован или срок доступа истёк'});req.securityUser=user;res.setHeader('Cache-Control','no-store');if(req.method!=='GET'&&req.path.startsWith('/admin-api/v1/platform/')){const target=req.path.match(/\/users\/([^/]+)/)?.[1]||'';res.on('finish',()=>{if(res.statusCode<400)pool.query('insert into platform_admin_audit(actor_id,actor_email,target,action,http_status) values($1,$2,$3,$4,$5)',[user.id,user.email,target,req.method+' '+req.path,res.statusCode]).catch(error=>console.error('Admin audit write failed',error))})}next()}catch(error){console.error('Admin authorization check failed',error);res.status(503).json({error:'Не удалось проверить состояние аккаунта'})}}
async function revokeTarget(req,res,next){try{await ensureSchema();await pool.query('update users set session_version=session_version+1 where id=$1',[req.params.id]);next()}catch(error){console.error('Session revocation failed',error);res.status(503).json({error:'Не удалось отозвать активные сессии'})}}
express.application.init=function(...args){const result=originalInit.apply(this,args);this.set('trust proxy','loopback');this.disable('x-powered-by');return result};
express.application.use=function(...handlers){if(!installed){installed=true;originalUse.call(this,currentAccount)}return originalUse.call(this,...handlers)};
express.application.post=function(path,...handlers){if(/^\/admin-api\/v1\/platform\/users\/:id\/(freeze|cancel|reset-password)$/.test(path)){const[first,second,...rest]=handlers;return originalPost.call(this,path,first,second,revokeTarget,...rest)}return originalPost.call(this,path,...handlers)};
