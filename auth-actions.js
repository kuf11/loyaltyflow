(()=>{const destination=label=>label==='войти'?'/login.html':['попробовать','запустить программу'].includes(label)?'/register.html':'';document.querySelectorAll('button').forEach(button=>{const url=destination(button.textContent.trim().toLowerCase());if(url)button.addEventListener('click',event=>{event.preventDefault();location.href=url})});const mode=new URLSearchParams(location.search).get('mode');if(mode==='login')location.replace('/login.html');if(mode==='reg')location.replace('/register.html')})();
