(()=>{function openAuth(mode){if(typeof window.tab==='function')window.tab(mode);document.getElementById('auth')?.scrollIntoView({behavior:'smooth',block:'start'});requestAnimationFrame(()=>document.querySelector('#'+mode+' input')?.focus())}document.querySelectorAll('button').forEach(button=>{const label=button.textContent.trim().toLowerCase();if(label==='войти')button.addEventListener('click',()=>openAuth('login'));if(['попробовать','запустить программу'].includes(label))button.addEventListener('click',()=>openAuth('reg'))});const mode=new URLSearchParams(location.search).get('mode');if(mode==='login'||mode==='reg')openAuth(mode)})();
