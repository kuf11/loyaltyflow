(()=>{const token=localStorage.getItem('LF_TOKEN'),text=document.getElementById('buttonText'),url=document.getElementById('buttonUrl'),preview=document.getElementById('previewButton');if(!token||!text||!url)return;let fallback='';fetch('/api/v1/owner/miniapp',{headers:{Authorization:'Bearer '+token}}).then(r=>r.ok?r.json():Promise.reject()).then(data=>{fallback=String(data.appUrl||'');if(fallback){url.placeholder='Необязательно — откроется Mini App внутри Telegram';syncPreview()}}).catch(()=>{});function syncPreview(){queueMicrotask(()=>{if(!preview||!text.value.trim()||url.value.trim()||!fallback)return;preview.href=fallback;preview.title='Откроет Mini App внутри Telegram'})}text.addEventListener('input',syncPreview);url.addEventListener('input',syncPreview);const fields=document.querySelector('.button-fields');if(fields&&!document.getElementById('scheduleType')){const box=document.createElement('div');box.className='button-fields';box.style.marginTop='12px';box.innerHTML='<label>Когда отправить<select id="scheduleType"><option value="now">Сейчас</option><option value="once">Один раз</option><option value="daily">Каждый день</option><option value="weekly">Раз в неделю</option><option value="monthly">Раз в месяц</option></select></label><label id="scheduleDateLabel" hidden>Дата и время по МСК<input id="scheduleDate" type="datetime-local"></label>';fields.after(box);const type=box.querySelector('#scheduleType'),label=box.querySelector('#scheduleDateLabel'),date=box.querySelector('#scheduleDate');const tomorrow=new Date(Date.now()+86400000);date.value=new Intl.DateTimeFormat('sv-SE',{timeZone:'Europe/Moscow',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit'}).format(tomorrow).replace(' ','T');type.onchange=()=>label.hidden=type.value==='now'}const originalFetch=window.fetch.bind(window);window.fetch=(input,options={})=>{if(String(input).includes('/admin-api/v1/broadcast/send')&&options.body){try{const body=JSON.parse(options.body),type=document.getElementById('scheduleType')?.value||'now',value=document.getElementById('scheduleDate')?.value||'';body.scheduleType=type;if(type!=='now'&&value)body.scheduleAt=new Date(value+':00+03:00').toISOString();options={...options,body:JSON.stringify(body)}}catch(error){}}return originalFetch(input,options)}})();
