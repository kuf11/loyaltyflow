(()=>{const options=document.querySelector('.schedule-options'),type=document.getElementById('scheduleType'),trigger=document.getElementById('calendarTrigger');if(!options||!type||!trigger)return;options.addEventListener('click',event=>{const button=event.target.closest('[data-schedule]');if(!button||event.isTrusted||button.dataset.schedule==='now')return;event.stopImmediatePropagation();type.value=button.dataset.schedule;options.querySelectorAll('.schedule-option').forEach(item=>item.classList.toggle('active',item===button));trigger.hidden=false},true)})();
