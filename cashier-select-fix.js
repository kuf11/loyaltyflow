(()=>{function init(){const select=document.getElementById('paymentMethod');if(!select||select.dataset.customized)return;select.dataset.customized='1';const wrap=document.createElement('div');wrap.className='payment-select';select.parentNode.insertBefore(wrap,select);wrap.append(select);const trigger=document.createElement('button');trigger.type='button';trigger.className='payment-trigger';const menu=document.createElement('div');menu.className='payment-menu';menu.hidden=true;wrap.append(trigger,menu);const label=()=>select.selectedOptions[0]?.textContent||'Выберите способ';function sync(){trigger.textContent=label();menu.querySelectorAll('.payment-option').forEach(btn=>btn.classList.toggle('selected',btn.dataset.value===select.value))}Array.from(select.options).forEach((option,index)=>{const btn=document.createElement('button');btn.type='button';btn.className='payment-option';btn.dataset.value=option.value;btn.textContent=(index===0?'✓ ':'')+option.textContent;btn.onclick=()=>{select.value=option.value;select.dispatchEvent(new Event('change',{bubbles:true}));close()};menu.append(btn)});function open(){wrap.classList.add('open');menu.hidden=false;sync()}function close(){wrap.classList.remove('open');menu.hidden=true;sync()}trigger.onclick=event=>{event.stopPropagation();wrap.classList.contains('open')?close():open()};document.addEventListener('click',event=>{if(!wrap.contains(event.target))close()});document.addEventListener('keydown',event=>{if(event.key==='Escape')close()});select.addEventListener('change',sync);sync()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init()})();
