(()=>{const button=document.getElementById('cashierLogout'),loginUrl=localStorage.getItem('LF_CASHIER_LOGIN_URL')||'/cashier-login.html';function leave(reason=''){localStorage.removeItem('LF_CASHIER_TOKEN');localStorage.removeItem('LF_CASHIER_NAME');localStorage.removeItem('LF_CASHIER_EXPIRES_AT');if(reason)sessionStorage.setItem('LF_CASHIER_LOGOUT_REASON',reason);location.replace(loginUrl)}button?.addEventListener('click',()=>leave(),{capture:true});function expiry(){const stored=Number(localStorage.getItem('LF_CASHIER_EXPIRES_AT'));if(Number.isFinite(stored)&&stored>0)return stored;try{const body=localStorage.getItem('LF_CASHIER_TOKEN')?.split('.')[0];return Number(JSON.parse(atob(body.replace(/-/g,'+').replace(/_/g,'/'))).exp)||0}catch{return 0}}const expiresAt=expiry();if(!expiresAt)return leave('Сессия завершена. Войдите снова.');const remaining=expiresAt-Date.now();if(remaining<=0)return leave('Прошло 6 часов. Войдите снова.');setTimeout(()=>leave('Прошло 6 часов. Войдите снова.'),Math.min(remaining,2147483647));document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible'&&Date.now()>=expiresAt)leave('Прошло 6 часов. Войдите снова.')})})();
