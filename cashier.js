(()=>{const token=localStorage.getItem('LF_CASHIER_TOKEN'),id=x=>document.getElementById(x),loginUrl=localStorage.getItem('LF_CASHIER_LOGIN_URL')||'/cashier-login.html';if(!token){location.replace(loginUrl);return}let qr='',customer=null,stream=null,scanning=false,operationId='';const api=async(path,opt={})=>{opt.headers={...(opt.headers||{}),Authorization:'Bearer '+token};const response=await fetch('/api/v1/cashier'+path,opt),data=await response.json().catch(()=>({}));if(response.status===401){localStorage.removeItem('LF_CASHIER_TOKEN');location.replace(loginUrl);throw Error('Сессия завершена')}if(!response.ok)throw Error(data.error||'Ошибка сервера ('+response.status+')');return data};function status(text,error=false){id('scanStatus').textContent=text;id('scanStatus').classList.toggle('error',error)}function stopCamera(){scanning=false;if(stream)stream.getTracks().forEach(track=>track.stop());stream=null;id('camera').hidden=true;id('cameraButton').textContent='Камера'}function render(data){customer=data.customer;operationId=crypto.randomUUID();id('customerCard').hidden=false;id('customerName').textContent=customer.name;id('customerMeta').textContent='Код: '+customer.code+(customer.phone?' · '+customer.phone:customer.username?' · @'+customer.username:'');id('avatar').textContent=(customer.name||'К')[0].toUpperCase();id('balance').textContent=Number(customer.balance).toLocaleString('ru-RU');id('balanceNote').textContent=data.balanceChanged?'QR содержал старый баланс — показано актуальное значение.':'Баланс подтверждён сервером.';id('redeemAmount').max=customer.balance;id('redeemAmount').value='';id('orderTotal').value='';id('paymentMethod').value='';id('result').innerHTML='';updateSummary();id('customerCard').scrollIntoView({behavior:'smooth',block:'start'})}async function lookup(value){const clean=String(value||'').trim();if(!clean)return status('Отсканируйте QR или введите код клиента',true);status('Проверяем клиента…');try{const data=await api('/customer?qr='+encodeURIComponent(clean));qr=clean;render(data);status('Клиент найден');stopCamera()}catch(error){customer=null;id('customerCard').hidden=true;status(error.message,true)}}id('scanForm').onsubmit=e=>{e.preventDefault();lookup(id('qrInput').value);id('qrInput').select()};function updateSummary(){const amount=Math.trunc(Number(id('redeemAmount').value)||0),total=Number(id('orderTotal').value)||0,method=id('paymentMethod').selectedOptions[0]?.textContent||'';id('summary').textContent=amount>0&&total>0&&id('paymentMethod').value?'Чек '+total.toLocaleString('ru-RU')+' ₽ · списать '+amount.toLocaleString('ru-RU')+' бонусов · '+method+'.':'Укажите сумму покупки, бонусы и способ оплаты.'}id('redeemAmount').oninput=updateSummary;id('orderTotal').oninput=updateSummary;id('paymentMethod').onchange=updateSummary;document.querySelectorAll('[data-part]').forEach(button=>button.onclick=()=>{if(!customer||customer.balance<1)return;id('redeemAmount').value=Math.max(1,Math.floor(customer.balance*Number(button.dataset.part)));updateSummary()});id('redeemButton').onclick=async()=>{if(!customer||!qr)return;const amount=Math.trunc(Number(id('redeemAmount').value)||0),orderTotal=Number(id('orderTotal').value)||0,paymentMethod=id('paymentMethod').value;if(orderTotal<=0)return status('Введите сумму покупки',true);if(amount<1||amount>customer.balance)return status('Проверьте сумму списания',true);if(!paymentMethod)return status('Выберите способ оплаты',true);if(!confirm('Списать '+amount.toLocaleString('ru-RU')+' бонусов у '+customer.name+'?'))return;const button=id('redeemButton');button.disabled=true;button.textContent='Списываем…';try{const data=await api('/redeem',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({qr,amount,orderTotal,paymentMethod,requestId:operationId})});customer=data.customer;id('balance').textContent=Number(customer.balance).toLocaleString('ru-RU');id('redeemAmount').max=customer.balance;id('result').innerHTML='<div class="success">Оплата подтверждена. Списано '+Number(data.redeemed).toLocaleString('ru-RU')+' бонусов. Остаток: '+Number(data.customer.balance).toLocaleString('ru-RU')+'.</div>';operationId=crypto.randomUUID();status('Операция завершена')}catch(error){status(error.message,true)}finally{button.disabled=false;button.textContent='Подтвердить списание'}};id('cameraButton').onclick=async()=>{if(scanning){stopCamera();return}if(!('BarcodeDetector'in window)||!navigator.mediaDevices?.getUserMedia)return status('Камера QR не поддерживается. Используйте сканер или введите код.',true);try{const detector=new BarcodeDetector({formats:['qr_code']});stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'}}});id('video').srcObject=stream;id('camera').hidden=false;id('cameraButton').textContent='Закрыть камеру';scanning=true;const tick=async()=>{if(!scanning)return;try{const codes=await detector.detect(id('video'));if(codes[0]?.rawValue){id('qrInput').value=codes[0].rawValue;await lookup(codes[0].rawValue);return}}catch{}requestAnimationFrame(tick)};requestAnimationFrame(tick)}catch{stopCamera();status('Не удалось открыть камеру. Разрешите доступ или используйте сканер.',true)}};id('logout').onclick=()=>{stopCamera();localStorage.removeItem('LF_CASHIER_TOKEN');localStorage.removeItem('LF_CASHIER_NAME');location.replace(loginUrl)};addEventListener('pagehide',stopCamera);api('/me').then(data=>{id('cashierName').textContent=data.cashier?.fullName||localStorage.getItem('LF_CASHIER_NAME')||'Кассир';id('qrInput').focus()}).catch(error=>status(error.message,true))})();
