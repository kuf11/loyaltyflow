(()=>{const rawFetch=window.fetch.bind(window);window.fetch=async(input,init)=>{const response=await rawFetch(input,init);try{const url=typeof input==='string'?input:input?.url||'';if(response.ok&&/\/loyalty\/state(?:\?|$)/.test(url)){const data=await response.clone().json();if(data?.customer&&!data.customer.phoneVerified){data.customer.balance=0;data.customer.registrationPending=true;if(Array.isArray(data.history))data.history=data.history.filter(item=>item.kind!=='welcome');const headers=new Headers(response.headers);headers.set('content-type','application/json');return new Response(JSON.stringify(data),{status:response.status,statusText:response.statusText,headers})}}}catch{}return response}})();
