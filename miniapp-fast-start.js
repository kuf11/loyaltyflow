(()=>{const app=document.getElementById('app');if(!app)return;const tenant=new URLSearchParams(location.search).get('tenant')||'main',cacheKey='lf_config_'+tenant;let cached=null;try{cached=JSON.parse(localStorage.getItem(cacheKey)||'null')}catch{}const design=cached?.design||{},name=String(design.name||'LoyaltyFlow'),logo=design.logo?`<img src="${String(design.logo).replace(/["<>]/g,'')}" alt="">`:'<span>L</span>';app.innerHTML=`<div class="fast-shell"><header>${logo}<div><b>${name.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}</b><small>Программа лояльности</small></div></header><main><div class="fast-card"></div><div class="fast-card short"></div><p>Загружаем данные…</p></main></div>`;const nativeFetch=window.fetch.bind(window);window.fetch=async(input,init)=>{const response=await nativeFetch(input,init);try{const url=typeof input==='string'?input:input?.url||'';if(response.ok&&url.includes('/api/v1/public/')&&url.endsWith('/config'))response.clone().json().then(data=>localStorage.setItem(cacheKey,JSON.stringify(data))).catch(()=>{})}catch{}return response}})();
