(()=>{const button=document.getElementById('refresh');if(!button)return;button.type='button';button.style.pointerEvents='auto';button.style.position='relative';button.style.zIndex='5';button.addEventListener('click',async event=>{event.preventDefault();event.stopImmediatePropagation();button.disabled=true;const label=button.textContent;button.textContent='Обновляем…';try{await load();notice('Список пользователей обновлён')}catch(error){notice(error.message,true)}finally{button.disabled=false;button.textContent=label}},true)})();
